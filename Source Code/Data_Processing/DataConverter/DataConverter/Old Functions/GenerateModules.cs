using KnowledgeDB;
using System;
using System.Collections.Generic;
using System.Text;

namespace DataConverter
{
    public class GenerateModules
    {
        /***
        This function creates a list of modules with predefined (hardcoded) names
        :return: list with all created modules
        ***/
        /*
        public static List<Module> GenerateMods()
        {
            List<Module> modules = new List<Module>
            {
                new Module("A1-A3"),
                new Module("A4"),
                new Module("B1"),
                new Module("B2"),
                new Module("C3"),
                new Module("C4"),
                new Module("D"),
            };
            //reverses order so that entries appear in correct way in the db (maybe doesn't help?)
            modules.Reverse();
            return modules;
        
        }
        */
    }
}
